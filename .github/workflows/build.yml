name: Build BTLEmap

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Build
        run: |
          xcodebuild \
            -project BLE-Scanner.xcodeproj \
            -scheme BLE-Scanner \
            -sdk iphoneos \
            -configuration Debug \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Check built app
        run: |
          ls -la build/Build/Products/Debug-iphoneos/
          test -d build/Build/Products/Debug-iphoneos/BLE-Scanner.app

      - name: Upload app
        uses: actions/upload-artifact@v4
        with:
          name: BTLEmap-app
          path: build/Build/Products/Debug-iphoneos/BLE-Scanner.app
          if-no-files-found: error